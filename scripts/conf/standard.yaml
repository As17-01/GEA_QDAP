defaults:
  - datasets: common
  - _self_

hydra:
  run:
    dir: .
  job:
    chdir: false

ga:
  _target_: src.algos.ga_standard.StandardGA
  population_size: 350
  iterations: 1000
  crossover_rate: 0.7
  mutation_rate: 0.3

run:
  runs: 1
  time_limit: 1000

output_file: results/standard.json
